import { DataMap } from "./DataMap";
export declare class DataMapExecutor {
    private cacheStore;
    private converter;
    exec(data: any, map: DataMap, isCollection: boolean): any;
    private getIns(map, data);
    private getCacheKey(map, data);
}
