import { DataMap } from "./DataMap";
import { DataMapMetadata } from "./metadata/DataMapMetadata";
/**
 * Use DataMapParser to parse DataMap
 */
export declare class DataMapParser {
    private _map;
    constructor(map: DataMap);
    parse(map: DataMap, root: boolean): DataMapMetadata;
    private parseResultsMap(results);
    private parseAssociationMap(associations);
    private parseCollectionMap(collections);
}
