/// <reference types="node" />
import { Klass } from "../core/Klass";
import { EventEmitter } from "events";
export declare class EventListenerMetadata {
    klass: Klass;
    method: string;
    eventName: string;
    emitterKlass: new (...args) => EventEmitter;
    handler: (...args) => any;
}
