import { FilterMetadata } from "./FilterMetadata";
export declare class FilterRegistry {
    private static _filters;
    static filters: Map<Function, FilterMetadata>;
    static registerFilter(type: Function): void;
    static getFilter(type: Function): FilterMetadata;
}
