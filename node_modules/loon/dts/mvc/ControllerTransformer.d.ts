/// <reference types="express" />
import { ControllerMetadata } from "./ControllerMetadata";
import * as Express from "express";
export declare class ControllerTransformer {
    private _controllerMetadata;
    private _router;
    readonly controllerMetadata: ControllerMetadata;
    constructor(controllerMetadata: ControllerMetadata);
    transform(): Express.Router;
    private getFiltersForAction(actionName);
}
