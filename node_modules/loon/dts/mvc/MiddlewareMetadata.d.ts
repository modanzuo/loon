import { HandlerMetadata } from "./HandlerMetadata";
import { MiddlewareOptions } from "./MiddlewareOptions";
export declare class MiddlewareMetadata {
    private _handler;
    private _type;
    private _order;
    private _baseUrl;
    private _isErrorMiddleware;
    readonly type: Function;
    readonly order: number;
    readonly baseUrl: string | RegExp;
    isErrorMiddleware: boolean;
    handler: HandlerMetadata;
    constructor(type: Function, options?: MiddlewareOptions);
    init(options?: MiddlewareOptions): this;
}
