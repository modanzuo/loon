"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const _ = require("lodash");
const Path = require("path");
class ConfigContainer {
    static registerConfig(path) {
        this.configFiles.push(path);
        try {
            const name = Path.parse(path).name;
            const config = require(path);
            this.config = _.merge({}, this.config, { [name]: config });
        }
        catch (err) {
            console.log(`[TYPED][ERR] ${path} load error`);
        }
    }
    static get(expression) {
        return _.get(this.config, expression);
    }
}
ConfigContainer.configFiles = [];
exports.ConfigContainer = ConfigContainer;
//# sourceMappingURL=ConfigContainer.js.map