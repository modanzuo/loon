"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class DataContainer {
    static registerColumn(type, columnName, propertyName) {
        this.columns.push({ type, columnName, propertyName });
    }
    static registerTable(type, tableName) {
        this.tables.push({ type, tableName });
    }
    static getTableName(type) {
        const table = this.tables.find(item => item.type === type);
        if (typeof table !== 'undefined') {
            return table.tableName;
        }
        else {
            throw new Error("[TYPED] => load table error");
        }
    }
    static getColumnsName(type) {
        this.columns
            .filter(item => item.type === type)
            .map(column => {
        });
    }
    static getColumns(type) {
        return this.columns
            .filter(item => item.type === type);
    }
}
DataContainer.columns = [];
DataContainer.tables = [];
exports.DataContainer = DataContainer;
//# sourceMappingURL=DataContainer.js.map